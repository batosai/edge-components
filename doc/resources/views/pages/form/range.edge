@layout()
  @slot('head')
    <title>Edge components range</title>
    <meta name="description" content="" />
  @end

  @!breadcrumb({
    items: [
      { label: 'Components' },
      { label: 'Range' }
    ]
  })

  @!section({
    title: 'Button',
    template: `@!jrmc.button({ text: 'Button' })`
  })

@end
