@layout()
  @slot('head')
    <title>Edge components toast</title>
    <meta name="description" content="" />
  @end

  @!breadcrumb({
    items: [
      { label: 'Components' },
      { label: 'Toast' }
    ]
  })

  @!section({
    title: 'Button',
    template: `@!jrmc.button({ text: 'Button' })`
  })

@end
