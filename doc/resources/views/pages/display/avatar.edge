@layout()
  @slot('head')
    <title>Edge components avatar</title>
    <meta name="description" content="" />
  @end

  @!breadcrumb({
    items: [
      { label: 'Components' },
      { label: 'Avatar' }
    ]
  })

  @!section({
    title: 'Button',
    template: `@!jrmc.button({ text: 'Button' })`
  })

@end
