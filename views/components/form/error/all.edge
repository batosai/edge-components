@if(flashMessages.all() && flashMessages.all().errors)
  @jrmc.alert.error($props)
    <ul>
    @each((error, index) in flashMessages.all().errors)
      <li> {{ index }}: {{ error }} </li>
    @end
    </ul>
  @end
@end

