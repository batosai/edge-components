@set('as', $props.has('as') ? $props.get('as') : 'label' )
@set('klass', $props.has('class') ? $props.get('class') : '')
@set('klass', Array.isArray(klass) ? klass.join(' ') : klass)

<{{as}} class="label {{ klass }}" {{ $props.serializeExcept(['class', 'as']) }}>
  @if($props.has('text'))~
    @!form.label.text({ text }) 
  @end~

  {{{ await $slots.main() || '' }}}
</{{as}}>