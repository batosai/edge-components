<input
  {{ $props.serializeExcept(['text', 'class', 'type']) }}
  type="{{ $props.has('type') ? $props.get('type') : 'button' }}"
  class="{{ jrmc.getCssClass($props, 'btn') }}"
  value="{{ text }}"
/>
