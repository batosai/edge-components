<div class="carousel{{ $props.has('class') ? ` ${$props.get('class')}` : ' rounded-box' }}" {{ $props.serializeExcept(['class']) }}>
  @if($props.has('items'))
    @each(item in items)
      @!carousel.item({ ...item })
    @end
  @else
    {{{ await $slots.main() || '' }}}
  @end
</div>